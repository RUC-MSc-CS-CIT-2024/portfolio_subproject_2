{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": {
    "PostgresConnection": "Host=${DB_HOST};Database=${DB_NAME};Username=${DB_USER};Port=${DB_PORT};Password=${DB_PASSWORD}"
  }
}
